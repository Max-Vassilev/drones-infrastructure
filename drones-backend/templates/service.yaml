apiVersion: v1
kind: Service
metadata:
  name: drones-backend-service
  labels:
    app: drones-backend
spec:
  type: ClusterIP
  selector:
    app: drones-backend
  ports:
    - port: 5001
      targetPort: 5001
